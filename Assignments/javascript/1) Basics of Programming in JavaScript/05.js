// declare constants 

const pi = 3.14; //we don't say that we have declared a variable. We have declared a constant
//pi = 3.15; this line will give error.
console.log(pi);

//we cannot change const values but we can do operations with it.
console.log(pi*2*2);//for example like this